| Decision | Reason | Impact | Date |
| --- | --- | --- | --- |
| Rebuild UI theo huong HTML/template thay vi React/Vite | User danh gia React/Vite cu cung va khong dat chat luong visual mong muon. | UI moi uu tien FastAPI templates/static assets, tan dung HTML/CSS co san. | 2026-03-25 |
| Su dung `BaseSource.AppUI` va `final_user_ui.html` lam nguon UI chinh | Da co giao dien san, tiet kiem thoi gian va bam sat workflow app cu. | Can map lai Razor/HTML thanh templates va noi API FastAPI vao. | 2026-03-25 |
| Backend van chot FastAPI + Python worker + Postgres + Redis | User xac nhan giu kien truc backend/worker/deploy da thong nhat. | Can rebuild backend sach tu dau, nhung contract phai phu hop worker VPS model. | 2026-03-25 |
| User page render qua `backend/app/routers/web.py` voi seed context in-memory | User yeu cau giu `main.py` va cac router API khac khong doi, chi tao template co the render an toan tu UI moi. | User dashboard co the render ngay bang FastAPI/Jinja, va sau nay co the thay seed bang store/DB ma khong can doi layout. | 2026-03-25 |
| Dung seed contract in-memory JSON-friendly cho `user`, `worker`, `channel`, `channel grant`, `render job`, `oauth summary` | Can bootstrap nhanh backend khi chua co DB/persistence layer. | `backend/app/schemas.py`, `backend/app/store.py`, `backend/app/routers/*` se lam source of truth tam thoi cho shell API. | 2026-03-25 |
| Chot `user_dashboard.html` + `user_dashboard.js` lam shell user duy nhat, bo template user du thua | Can mot nguon UI duy nhat de noi backend va khong quay lai React/Vite. | `/app` va `/` bam theo HTML/CSS da chot; JS chi lam adapter cho form/job action. | 2026-03-25 |
| Admin FastAPI phai bam shell Elevated SaaS cua `BaseSource.AppUI` thay vi giu dashboard bootstrap tam | User da nang cap toan bo UI admin trong repo .NET cu va yeu cau noi backend vao dung giao dien nay. | FastAPI admin duoc tach thanh 4 route `user/bot/channel/render`, mount lai asset `admin-themes` + `css`, va `/admin` redirect ve `/admin/user/index`. | 2026-03-25 |
| `final_user_ui.html` la visual source of truth cho toan bo du an | User xac nhan can thong nhat visual language va khong muon admin tiep tuc bam style Elevated cua app cu. | Moi tai lieu UI va cac man hinh admin/user ve sau phai tham chieu `final_user_ui.html`; repo .NET cu chi con vai tro tham chieu luong nghiep vu. | 2026-03-25 |
| UI dung `BOT` thay cho `worker` trong tat ca nhan hien thi | User muon ng√¥n nguoi nhat quan giua admin va user, trong khi backend model worker van can giu nguyen de khong vo contract. | Cac template/JS se hien thi `BOT`, con schema/store/route backend van luu ten `worker` o cap du lieu. | 2026-03-25 |
| Admin UI giu workflow cu nhung gom du lieu vao cot thong tin tong hop thay vi trai raw field thanh qua nhieu cot | Admin can de them CRUD/parity logic sau nay ma khong quay lai layout roi, kho scan. | Cac man `User / BOT / Channel / Render` uu tien nhom metadata theo hang, chip trang thai va action compact, nhung khong doi route hay contract du lieu. | 2026-03-25 |
| User Management parity se bam sat app cu cho muc 1.1-1.7, con muc 1.8 dung page mapping BOT rieng tren FastAPI | Snapshot .NET cu cho `managerbot` khong nhat quan giua controller/view/service; can chot mot luong ro rang de tiep tuc parity ma khong treo o artifact do dang. | Module user co flow CRUD va mapping BOT dung duoc ngay tren local; cac module 2-5 co the tiep tuc tren cung visual/system contract nay. | 2026-03-25 |
| BOT relation pages trong FastAPI bam direct user-BOT mapping | Snapshot .NET cu suy dien user/BOT qua channel va controller khong nhat quan, trong khi module 1 da chot flow mapping BOT rieng. | Module 2.5 va 2.6 co page va action ro rang, thong nhat voi contract hien tai va de noi module channel ve sau. | 2026-03-25 |
| Channel Management dung mapping `channel_user_links` rieng thay vi suy dien tu BOT | Luong cap quyen kenh cua app cu co page va action rieng (`channel/user`, `channel/users`, `updateuserchannel`, `adduser`), nen suy dien tu BOT se lam sai nghiep vu. | Module 3 co the add/remove user tren kenh doc lap, DS User cua kenh dung du lieu that, va action xoa BOT khong con de lai mapping kenh rac. | 2026-03-25 |
| Render detail trong FastAPI giu kieu readonly config-first | App cu hien man `renderinfo` de doc lai cau hinh input, khong phai runtime diagnostics. | Module 4 chi hien Intro/VideoLoop/AudioLoop/Outro, channel, time render, schedule va title; khong mo rong thanh man debug khac he. | 2026-03-25 |
| Shared admin auth dung session-cookie va role gate theo route | Can dat parity voi app cu (`Admin`, `Manager`) nhung khong muon refactor vo lon toan bo router/template shell. | Admin web/API duoc bao ve bang session that, manager bi gioi han scope, va login/logout co the chay local ngay. | 2026-03-25 |
| Persistence tam thoi dung SQLite JSON snapshot truoc khi chuyen sang Postgres + Redis | Muc tieu hien tai la lam cho local bootstrap/admin prototype khong mat du lieu sau restart ma khong rewrite toan bo repository sang relational layer ngay. | State admin/user/channel/render duoc luu that o local, module 5 hoan tat, nhung van can migration plan khi chuyen production data layer. | 2026-03-25 |
| Google OAuth local bootstrap luu `refresh token` tren `ChannelRecord` trong SQLite snapshot va tu nap `.env` tai root workspace | Can kich hoat flow connect that ngay trong app hien tai ma chua co secret manager/DB production. | User co the connect YouTube channel that tren local bang `.env`; doi lai token hien dang duoc giu o local bootstrap layer va can migration/encryption truoc khi dua production. | 2026-03-25 |
| Worker media moi phai giu fast-path `copy/remux` cho case loop hop le, chi normalize/re-encode khi that su can | Trace app cu cho thay gia tri lon nhat cua pipeline hien tai nam o viec ghep loop bang `FFProbe + ffmpeg -c copy`, giup job 4K dai van kha nhe neu input dong nhat. | Ban rebuild can uu tien detect input "compatible" de di fast-path, dong thoi tach rieng fallback normalize/re-encode cho case sai fps/resolution/codec. | 2026-03-25 |
| Local upload cho file lon se di qua `upload session + resumable chunk upload`, job creation chi nhan asset refs da upload xong | `POST /user/jobs` kieu multipart don khoi khong chiu duoc file lon va mang chap chon, trong khi mo hinh host + worker can asset lifecycle ro rang. | Frontend phai upload chunk truoc, backend phai co session/asset layer, va ve sau co the doi storage backend ma khong vo contract tao job. | 2026-03-25 |
| Worker moi phai la agent outbound co `register + heartbeat` toi control plane, khong push job qua connection state nhu app .NET cu | Muc tieu mo rong 2 -> 50 worker can mo hinh pull/heartbeat on dinh hon push SignalR g·∫Øn ch·∫∑t connection. | Da co scaffold `api/workers/*` va `workers/agent/main.py`; ve sau se mo rong them claim/progress/complete tren cung contract nay. | 2026-03-25 |
| Worker process that se duoc mo khoa theo 2 lop: contract `claim/progress/complete/fail` truoc, render/upload media that sau | Host shared cua user can uu tien route/job orchestration an toan truoc khi bat worker dong vao job that; dong thoi can co `simulate mode` de dien tap khong pha data. | Da co `simulate mode` opt-in va worker se khong tu y nhan job that tren VPS cho den khi chu dong bat env va noi FFmpeg/YouTube logic. | 2026-03-26 |
| Domain cho app host shared se di qua Caddy chung hien co va reverse proxy sang app Python o `172.17.0.1:8000` | Host da co nhieu app va mot Caddy container dang nam doc quyen `80/443`; them site block moi vao stack chung an toan hon mo them reverse proxy rieng. | Server-side route cho `ytb.jazzrelaxation.com` da duoc gan ma khong dung vao app khac; ban than HTTPS van phu thuoc DNS public tro dung ve host. | 2026-03-26 |
| Local upload asset se duoc worker tai qua control plane bang worker-authenticated route thay vi share disk hay mount chung | Worker VPS khong co duong dan local cua host, trong khi job hien chi luu `file_name`; route stream qua backend la cach it xao tron nhat de mo khoa pipeline that. | Backend bo sung `GET /api/workers/jobs/{job_id}/assets/{slot}`, worker co the tai local asset an toan ma khong can doi ha tang luu tru. | 2026-03-26 |
| Worker render that phai co safety gate `WORKER_EXECUTE_JOBS` va mac dinh `false` khi deploy | Host hien van co seed/demo jobs; bat runner that ngay tren VPS se de an nham queue cu va fail hang loat. | Co the deploy code render/download/ffmpeg len worker truoc, xac nhan service/heartbeat on dinh, roi moi bat render that co chu dich theo env. | 2026-03-26 |
| YouTube upload se duoc rollout bang gate rieng `WORKER_UPLOAD_TO_YOUTUBE`, mac dinh `false` cho den khi OAuth production san sang | Worker da co the render that, nhung host/channel hien chua dam bao day du `GOOGLE_CLIENT_ID/SECRET` va refresh token that cho moi kenh; can tranh bien render pipeline thanh point of failure moi qua som. | Co the deploy code upload YouTube truoc, verify khong vo luong render hien tai, va bat dan theo worker/channel khi user hoan tat OAuth production. | 2026-03-26 |
| Worker upload YouTube se dung YouTube Data API resumable upload truc tiep qua `httpx`, khong dua them Google client library vao runtime | Muc tieu hien tai la giu worker gon, de deploy, va chu dong kiem soat progress/chunk upload tren VPS. | Worker co them module `youtube_uploader.py`, control plane bo sung route cap upload target, va ve sau co the mo rong retry/resume ma khong phu thuoc client lib nang. | 2026-03-26 |
| Host web app tren shared VPS se duoc giu bang `systemd` service rieng thay vi process `uvicorn` detach tam thoi | Su co `502` xay ra khi origin `:8000` mat process, trong khi Caddy va Cloudflare van on; can co supervisor rieng cho app nay ma khong dung vao app khac. | Runtime web cua `youtube-upload-lush` co `systemctl enable --now youtube-upload-web.service`, tu restart khi roi process va giam rui ro `502` sau deploy/reboot. | 2026-03-26 |
| Homepage va legal pages public se duoc uu tien de phuc vu Google OAuth brand verification | Google reject homepage redirect login va privacy page loi; can mot bo trang public, HTML co ban, co lien ket ro rang giua home/privacy/terms. | App giu `root /` cho login flow, con homepage public chuyen sang `/home`; `privacy-policy` va `terms-of-service` tiep tuc public tren domain app. | 2026-03-26 |
| App runtime timezone mac dinh la `Asia/Saigon` va duoc cho phep override bang `APP_TIMEZONE` | User van hanh theo gio Ha Noi, trong khi host shared khong cung timezone lam job `scheduled_at` bi treo o `pending`. | Queue claim, worker progress, upload session va cac timestamp nghiep vu dung cung mot chuan gio; can giu nhat quan khi rollout qua host/worker khac. | 2026-03-26 |
| Rollout upload YouTube that chi mo khoa tren `worker-01`, giu `worker-02` lam standby | Da co upload thanh cong tren job thuc te, nhung van can giu 1 worker an toan de rollback/so sanh neu phat sinh loi YouTube API hoac quota. | Job moi gan `worker-01` se di het toi YouTube; `worker-02` van chi heartbeat/render fallback cho den khi rollout tiep. | 2026-03-26 |
| User dashboard phai hien tach 2 phase `render` va `upload` thay vi map truc tiep `job.status=completed` thanh `Hoan tat` | Sau khi mo khoa upload that, mot job co the da render xong nhung chua upload, hoac da len YouTube; UI cu che mat khac biet nay va gay nham lan trong van hanh. | Badge/status, progress color, timeline va action tren bang render duoc derive tu `output_url` va moc `upload_started_at/completed_at` ma khong doi contract worker/control plane. | 2026-03-26 |
| User queue realtime dung polling endpoint rieng thay vi reload ca trang | User can theo doi phase `render/upload` lien tuc; reload full page se mat context form, search va trai nghiem queue. | Control plane bo sung `GET /api/user/dashboard/live`, frontend poll 5 giay/l·∫ßn va patch lai KPI/tab/tbody ma khong doi shell hay route web chinh. | 2026-03-26 |
| Local asset cache va worker render output phai duoc don theo lifecycle job | User can tranh day o dia tren VPS; source media chi can giu den luc job den lich va render output chi can ton tai den khi upload YouTube xong. | Job hen lich tiep tuc giu source asset trong cache den khi den gio claim; job da upload thanh cong se chuyen preview sang YouTube thumbnail va xoa local source/output de giai phong dung luong. | 2026-03-26 |
| Job preview phai duoc cache thanh asset rieng tren control plane | Preview YouTube ngoai co the 404/khong on dinh, con source video local se bi xoa sau cleanup nen UI se mat anh neu khong co preview rieng. | Worker se trich frame tu media/output va upload ve control plane; job xoa thi preview cung bi xoa theo, giai quyet preview ben vung ma van don rac dung lifecycle. | 2026-03-26 |
| Admin login se giu contract session `/admin/login` hien tai nhung dung UI shell tu `login_preview.html` | User muon noi man hinh login moi truoc, trong khi flow auth admin/manager da co session gate va dang duoc route admin su dung on dinh. | Co the thay shell login ngay ma khong doi contract auth hien co; user-side auth that se duoc tach thanh bai toan rieng thay vi tron vao login admin. | 2026-03-26 |
| Phase auth moi se tach identity/account ra bang quan he trong cung SQLite, nhung giu jobs/workers/upload pipeline o snapshot hien tai | Can co user/admin session that, password hash va channel grant per-user ngay bay gio ma khong muon pha runtime render/upload dang on dinh. | `auth_users`, `auth_credentials`, `auth_channel_grants` tro thanh source of truth cho auth; job/worker state tiep tuc song song trong snapshot cho den khi co migration plan day du. | 2026-03-26 |
| Login web se dung mot cong vao duy nhat `/login`, roi dieu huong theo role sau khi xac thuc | Hai man login rieng user/admin gay bat tien va tao cam giac ton tai hai he thong tach biet, trong khi account role da nam chung mot data model. | User login xong vao `/app`; admin/manager login xong vao `/admin/*`; `/admin/login` chi con la alias redirect ve `/login?next=/admin/user/index`. | 2026-03-26 |
| Worker display name tren admin/user/export se dung `worker.name = IP VPS that`, con `worker.id` giu lam internal contract | Van can admin nhin vao la biet may VPS nao phai xu ly, nhung khong duoc pha cac flow `claim`, filter va mapping hien dang dua vao `worker_id`. | Moi UI/data view hien worker theo IP VPS; hidden field, query param va runtime contract tiep tuc dung `worker.id` de giu backward compatibility. | 2026-03-26 |
| Admin manager filter mac dinh phai tro ve `Tat ca manager`, khong sticky theo session cu; dropdown admin cung su dung cung mot visual language voi workspace | Filter manager dang bi luu lai theo session va mot so `select` native trong admin lech he thiet ke so voi workspace, gay cam giac tu dong bi khoa vao 1 manager va UI khong dong bo. | Route/admin shell se giu `[]` nhu mot all-state that, manager login van bi khoa scope rieng; cac `select.toolbar-select` 1 lua chon duoc enhance thanh custom dropdown dung layout, spacing va state cung he. | 2026-03-27 |
| Tat ca bang list admin se dung cung mot lop client-side `search + pagination + footer summary + delete page`, con export CSV phai tra file download co filename co dinh | Admin dang co nhieu table lech nhau ve kha nang tim kiem/phan trang, va route export hien tai tra response download khong on dinh ten file tren browser. | UI admin co chung mot behavior bang list nhu workspace render, de mo rong them table moi khong phai viet lai tu dau; route export duoc harden bang `Content-Disposition` + CSV payload on dinh de browser tai dung file. | 2026-03-27 |
| Bang admin se dung chung mot engine client-side cho `sort + search + pagination`, va search bar cua bang duoc dat trong chinh header panel | User muon toan bo table admin co hanh vi giong workspace render va khong muon thanh tim kiem nam tach thanh mot hang rieng phia duoi header. | `admin_tables.js` tro thanh lop chung cho moi list panel, header table co sort state ro rang, va toolbar search nam ngay canh title/section-note de dong bo he UI hien co. | 2026-03-27 |
| Form cap role manager/admin phai validate username va luon redirect ve page role kem notice ro rang | User thao tac tren UI role list can thay ket qua/loi ro, khong duoc roi vao `KeyError` hoac redirect im lang khi username trong/sai. | Route `updaterolemanager` va `updateroleadmin` duoc boc validation trong `try`, notice hien ngay tren trang role list, va backend tra message tieng Viet ro nghia cho case thanh cong/that bai. | 2026-03-27 |
| C?t `Lu?ng` c?a admin BOT list hi?n th? theo d?ng `dang ch?y / t?i da`, cÚn `Bang thÙng` l?y t? heartbeat telemetry th?t c?a worker | App cu d˘ng `NumberOfThreadsInRun / NumberOfThreads` theo nghia slot x? l˝ song song; c?t bang thÙng tru?c d‚y dang hard-code `0` nÍn g‚y hi?u nh?m | Admin nhÏn d˙ng tr?ng th·i v?n h‡nh hi?n t?i c?a VPS, cÚn worker heartbeat mang s? li?u m?ng th?t thay vÏ gi· tr? gi? l?p | 2026-03-27 |
| `threads` cua worker se tro thanh gioi han concurrency that duoc control plane cap phat, worker tu cap nhat theo heartbeat/register response va heartbeat dong thoi gia han lease cho moi job active | Neu chi bat thread pool o worker ma khong sua logic claim/lease, job se bi claim trung hoac het lease giua chung; dong bo desired threads qua response giup nut `Lu?ng` co hieu luc that ma khong can SSH sua env moi lan | BOT co the xu ly nhieu job song song an toan theo muc `threads`, control plane khong claim qua quota, va lease job on dinh hon trong suot thoi gian download/render/upload | 2026-03-27 |
| Worker SKU hien tai (4 vCPU / ~6 GB RAM) mac dinh nen van hanh o 2 luong | Bai test live 2 luong/worker cho thay ca 2 may deu claim 2 job cung luc; worker-01 hoan tat 2 upload thuc te, con worker-02 dung loi quota YouTube ngoai he thong chua phan anh gioi han CPU/RAM. | Mac dinh van hanh cho worker-01 va worker-02 la WORKER_THREADS=2, WORKER_CAPACITY=2; chua khuyen nghi nang len 3+ khi chua co bai soak test rieng voi video dai va upload that. | 2026-03-27 |

My Channel row dung worker/IP note thay bot badge | Bot badge lam row bi om do va lech mau so voi mau moi; worker/IP note g·ªçn hon va dung context van hanh | User workspace sat mau hon va top layout gon hon, can giu cung pattern nay cho cac polish UI tiep theo | 2026-03-27

| User render list khong duoc fallback sang Google Drive thumbnail suy doan tu raw file link | Thumbnail endpoint cua Drive co the 404 du file con ton tai, gay vo preview trong render list va spam request loi moi lan row rerender. | Job Drive khi chua co preview that se quay ve icon/placeholder; khi worker/control plane co thumbnail that thi moi phat `preview_url` len UI. | 2026-03-27 |

| User render list se hien phase `Download` rieng va chi doi sang `Render/Upload` sau khi worker tai xong toan bo asset | Job Drive lon tai ve VPS lau nhung UI cu chi co 2 thanh `render/upload`, tao cam giac nhu worker da vao render du du lieu van dang download. | Dashboard user phan tach ro phase tai nguon va phase xu ly, can worker bao progress download theo byte va store expose them `progress_mode/download_progress`. | 2026-03-29 |
| Control plane se tra 409/404 cho worker khi job da cancelled/completed/error hoac bi xoa, de worker tu dung va don temp/output | Neu worker van gui progress/lay asset sau khi user huy-xoa job, file tai ve va file render co the bi bo lai tren VPS. | Flow cancel/delete gio tro thanh stop-signal that cho worker; temp dir va output dang xu ly se duoc cleanup nhanh hon va khong tiep tuc chiem dung luong sau khi job bi huy/xoa. | 2026-03-29 |
Admin dynamic tables use DOM refresh + delegated dialog actions | Generic admin table engine was originally static after first render, so live BOT metrics and dynamic action buttons would desync after row replacement. | Admin pages can refresh tbody in place without breaking sort/search/pagination or dialog actions; reused for BOT live telemetry first. | 2026-03-29
Admin render filtering phai resolve manager theo quan he worker/channel thay vi raw job.manager_name | Du lieu live co the lech manager username giua job cu, channel va worker sau cac lan doi ten manager, khien admin render list bo sot job du store.jobs van con day du. | Scope filter va row metadata cua admin render page on dinh hon truoc du lieu lich su/stale names, khong can migration du lieu ngay lap tuc moi nhin thay du queue thuc. | 2026-03-29

| Worker heartbeat chi duoc phep gia han lease cho active_job_ids ma worker bao cao | Heartbeat cu gia han tat ca claimed active jobs, khien job bi mat thread sau restart van mac ket o downloading/uploading du worker khong con xu ly. | Control plane co the tu phuc hoi stale uploads thanh completed neu video da len YouTube, va khong con giu treo lease cho job worker da mat tracking. | 2026-03-29 |
| Go bo YoutubeBOTUpload-master/ khoi workspace va thay moi compatibility asset bang ackend/app/static | Thu muc .NET cu khong con can thiet, trong khi runtime van con vai fallback avatar/static path can giu on dinh sau khi xoa repo tham chieu. | Repo gon hon, GitHub khong con mang source cu, va app van giu duoc path /legacy + /admin-themes bang asset noi bo toi thieu. | 2026-03-30 |
