| Decision | Reason | Impact | Date |
| --- | --- | --- | --- |
| Rebuild UI theo huong HTML/template thay vi React/Vite | User danh gia React/Vite cu cung va khong dat chat luong visual mong muon. | UI moi uu tien FastAPI templates/static assets, tan dung HTML/CSS co san. | 2026-03-25 |
| Su dung `BaseSource.AppUI` va `final_user_ui.html` lam nguon UI chinh | Da co giao dien san, tiet kiem thoi gian va bam sat workflow app cu. | Can map lai Razor/HTML thanh templates va noi API FastAPI vao. | 2026-03-25 |
| Backend van chot FastAPI + Python worker + Postgres + Redis | User xac nhan giu kien truc backend/worker/deploy da thong nhat. | Can rebuild backend sach tu dau, nhung contract phai phu hop worker VPS model. | 2026-03-25 |
| User page render qua `backend/app/routers/web.py` voi seed context in-memory | User yeu cau giu `main.py` va cac router API khac khong doi, chi tao template co the render an toan tu UI moi. | User dashboard co the render ngay bang FastAPI/Jinja, va sau nay co the thay seed bang store/DB ma khong can doi layout. | 2026-03-25 |
| Dung seed contract in-memory JSON-friendly cho `user`, `worker`, `channel`, `channel grant`, `render job`, `oauth summary` | Can bootstrap nhanh backend khi chua co DB/persistence layer. | `backend/app/schemas.py`, `backend/app/store.py`, `backend/app/routers/*` se lam source of truth tam thoi cho shell API. | 2026-03-25 |
| Chot `user_dashboard.html` + `user_dashboard.js` lam shell user duy nhat, bo template user du thua | Can mot nguon UI duy nhat de noi backend va khong quay lai React/Vite. | `/app` va `/` bam theo HTML/CSS da chot; JS chi lam adapter cho form/job action. | 2026-03-25 |
| Admin FastAPI phai bam shell Elevated SaaS cua `BaseSource.AppUI` thay vi giu dashboard bootstrap tam | User da nang cap toan bo UI admin trong repo .NET cu va yeu cau noi backend vao dung giao dien nay. | FastAPI admin duoc tach thanh 4 route `user/bot/channel/render`, mount lai asset `admin-themes` + `css`, va `/admin` redirect ve `/admin/user/index`. | 2026-03-25 |
| `final_user_ui.html` la visual source of truth cho toan bo du an | User xac nhan can thong nhat visual language va khong muon admin tiep tuc bam style Elevated cua app cu. | Moi tai lieu UI va cac man hinh admin/user ve sau phai tham chieu `final_user_ui.html`; repo .NET cu chi con vai tro tham chieu luong nghiep vu. | 2026-03-25 |
| UI dung `BOT` thay cho `worker` trong tat ca nhan hien thi | User muon ngÃ´n nguoi nhat quan giua admin va user, trong khi backend model worker van can giu nguyen de khong vo contract. | Cac template/JS se hien thi `BOT`, con schema/store/route backend van luu ten `worker` o cap du lieu. | 2026-03-25 |
| Admin UI giu workflow cu nhung gom du lieu vao cot thong tin tong hop thay vi trai raw field thanh qua nhieu cot | Admin can de them CRUD/parity logic sau nay ma khong quay lai layout roi, kho scan. | Cac man `User / BOT / Channel / Render` uu tien nhom metadata theo hang, chip trang thai va action compact, nhung khong doi route hay contract du lieu. | 2026-03-25 |
| User Management parity se bam sat app cu cho muc 1.1-1.7, con muc 1.8 dung page mapping BOT rieng tren FastAPI | Snapshot .NET cu cho `managerbot` khong nhat quan giua controller/view/service; can chot mot luong ro rang de tiep tuc parity ma khong treo o artifact do dang. | Module user co flow CRUD va mapping BOT dung duoc ngay tren local; cac module 2-5 co the tiep tuc tren cung visual/system contract nay. | 2026-03-25 |
| BOT relation pages trong FastAPI bam direct user-BOT mapping | Snapshot .NET cu suy dien user/BOT qua channel va controller khong nhat quan, trong khi module 1 da chot flow mapping BOT rieng. | Module 2.5 va 2.6 co page va action ro rang, thong nhat voi contract hien tai va de noi module channel ve sau. | 2026-03-25 |
| Channel Management dung mapping `channel_user_links` rieng thay vi suy dien tu BOT | Luong cap quyen kenh cua app cu co page va action rieng (`channel/user`, `channel/users`, `updateuserchannel`, `adduser`), nen suy dien tu BOT se lam sai nghiep vu. | Module 3 co the add/remove user tren kenh doc lap, DS User cua kenh dung du lieu that, va action xoa BOT khong con de lai mapping kenh rac. | 2026-03-25 |
| Render detail trong FastAPI giu kieu readonly config-first | App cu hien man `renderinfo` de doc lai cau hinh input, khong phai runtime diagnostics. | Module 4 chi hien Intro/VideoLoop/AudioLoop/Outro, channel, time render, schedule va title; khong mo rong thanh man debug khac he. | 2026-03-25 |
| Shared admin auth dung session-cookie va role gate theo route | Can dat parity voi app cu (`Admin`, `Manager`) nhung khong muon refactor vo lon toan bo router/template shell. | Admin web/API duoc bao ve bang session that, manager bi gioi han scope, va login/logout co the chay local ngay. | 2026-03-25 |
| Persistence tam thoi dung SQLite JSON snapshot truoc khi chuyen sang Postgres + Redis | Muc tieu hien tai la lam cho local bootstrap/admin prototype khong mat du lieu sau restart ma khong rewrite toan bo repository sang relational layer ngay. | State admin/user/channel/render duoc luu that o local, module 5 hoan tat, nhung van can migration plan khi chuyen production data layer. | 2026-03-25 |
| Google OAuth local bootstrap luu `refresh token` tren `ChannelRecord` trong SQLite snapshot va tu nap `.env` tai root workspace | Can kich hoat flow connect that ngay trong app hien tai ma chua co secret manager/DB production. | User co the connect YouTube channel that tren local bang `.env`; doi lai token hien dang duoc giu o local bootstrap layer va can migration/encryption truoc khi dua production. | 2026-03-25 |
| Worker media moi phai giu fast-path `copy/remux` cho case loop hop le, chi normalize/re-encode khi that su can | Trace app cu cho thay gia tri lon nhat cua pipeline hien tai nam o viec ghep loop bang `FFProbe + ffmpeg -c copy`, giup job 4K dai van kha nhe neu input dong nhat. | Ban rebuild can uu tien detect input "compatible" de di fast-path, dong thoi tach rieng fallback normalize/re-encode cho case sai fps/resolution/codec. | 2026-03-25 |
| Local upload cho file lon se di qua `upload session + resumable chunk upload`, job creation chi nhan asset refs da upload xong | `POST /user/jobs` kieu multipart don khoi khong chiu duoc file lon va mang chap chon, trong khi mo hinh host + worker can asset lifecycle ro rang. | Frontend phai upload chunk truoc, backend phai co session/asset layer, va ve sau co the doi storage backend ma khong vo contract tao job. | 2026-03-25 |
| Worker moi phai la agent outbound co `register + heartbeat` toi control plane, khong push job qua connection state nhu app .NET cu | Muc tieu mo rong 2 -> 50 worker can mo hinh pull/heartbeat on dinh hon push SignalR gáº¯n cháº·t connection. | Da co scaffold `api/workers/*` va `workers/agent/main.py`; ve sau se mo rong them claim/progress/complete tren cung contract nay. | 2026-03-25 |
| Worker process that se duoc mo khoa theo 2 lop: contract `claim/progress/complete/fail` truoc, render/upload media that sau | Host shared cua user can uu tien route/job orchestration an toan truoc khi bat worker dong vao job that; dong thoi can co `simulate mode` de dien tap khong pha data. | Da co `simulate mode` opt-in va worker se khong tu y nhan job that tren VPS cho den khi chu dong bat env va noi FFmpeg/YouTube logic. | 2026-03-26 |
| Domain cho app host shared se di qua Caddy chung hien co va reverse proxy sang app Python o `172.17.0.1:8000` | Host da co nhieu app va mot Caddy container dang nam doc quyen `80/443`; them site block moi vao stack chung an toan hon mo them reverse proxy rieng. | Server-side route cho `ytb.jazzrelaxation.com` da duoc gan ma khong dung vao app khac; ban than HTTPS van phu thuoc DNS public tro dung ve host. | 2026-03-26 |
| Recovery sync tu VPS chi dong bo source dang chay, khong mirror runtime artifact hay repo-only file | Thu muc /opt/youtube-upload-lush la deploy copy khong co .git, co the thieu file repo-only va chua runtime artifact/backups; mirror thang se lam sach sai repo. | Khi can lay code nguoc tu VPS, chi sync backend/workers/infra/scripts/docs va root source can thiet, loai YoutubeBOTUpload-master, .env*, .venv, data, worker-data, .backup, .bak*. | 2026-03-30 |
| Deploy production se chay theo mo hinh git-first checkout, runtime tach rieng bang symlink | Thu muc /opt/youtube-upload-lush da bi sua nong va copy tay qua nhieu lan, gay lech giua local/GitHub/VPS va kho rollback. | Control-plane/worker se clone reset tu origin/main, con .env/.venv/backend-data/worker-data/.backup song o /opt/youtube-upload-lush-runtime; future deploy chi con bootstrap script + restart service. | 2026-03-30 |
| User workspace uu tien flow `browser session + noVNC` de them kenh, con OAuth giu lai lam fallback | Bai toan quota/publish verification khien user muon tu dang nhap Chromium tren Ubuntu thay vi redirect sang Google OAuth tren browser local. | Backend phai quan ly session Chromium/noVNC, channel record can luu them metadata `browser_profile`, va deploy host phai co GUI/runtime packages tren Ubuntu. | 2026-03-30 |
| Browser session phai chay tren worker/VPS duoc gan cho user, khong chay tren control plane | User se render va upload tren cung mot VPS; copy profile Chromium giua cac VPS sau khi dang nhap la kem ben va ton thoi gian. | Control plane chi giu session request + state; worker agent phai poll/sync browser session, tu launch Chromium/noVNC tren VPS cua no, va user dashboard mo link noVNC cua chinh worker do. | 2026-03-30 |
| Moi user chi duoc gan 1 worker/VPS tai mot thoi diem, va kenh/job chi hop le tren worker do | User xac nhan render va upload phai cung nam tren 1 VPS vi file render rat lon; worker khac khong nen render/upload thay. | `user_worker_links` phai duoc chuan hoa thanh mapping 1-1, dashboard user chi hien kenh/job cua worker duoc cap, va neu doi VPS thi user can reconnect kenh tren VPS moi truoc khi tao job tiep. | 2026-03-30 |

| VPS deploy repo `/opt/youtube-upload-lush` tam thoi la source of truth khi GitHub drift | User xac nhan workflow hien tai sua nong truc tiep tren VPS la thuc te van hanh va muon GitHub phan anh dung trang thai do. | Khi phat hien lech local/GitHub/VPS, uu tien keo source tu VPS ve local roi commit/push; chi loai tru runtime artifact nhu `.env`, `.venv`, `backend/app/data`, `worker-data`. | 2026-03-31 |
| Luong cap phat BOT cua admin se duoc tach thanh tab sidebar rieng `Cáº¥p phÃ¡t BOT`, con trang `Danh sÃ¡ch BOT` chi giu vai tro quan sat/van hanh | Panel cap phat dang chen thang vao man list BOT lam mat do thao tac cao, kho scan va khong phu hop khi cap/doi BOT lien tuc. | Sidebar admin co them diem vao rieng cho cap phat; route web va context du lieu duoc tach, while link tu man user/BOT se tro ve tab moi de giu mot noi thao tac tap trung. | 2026-03-31 |
| Man `Cáº¥p phÃ¡t BOT` duoc phep dung layout split-dispatch bÃ¡m mockup thay vi KPI strip mac dinh cua admin | Screen nay la man dieu phoi thao tac, user muon bÃ¡m sat file `bot_assignment_ui.html` va uu tien khong gian cho form + kho BOT hon la KPI. | UI system admin co mot ngoai le ro rang cho man nay: giu sidebar/palette/font cua app, nhung bo KPI va dung thanh tieu de mong + split panel trai/phai. | 2026-03-31 |
| Man `Cáº¥p phÃ¡t BOT` se bÃ¡m sat mockup workspace dieu phoi voi bot pool trai, rail chuyen giua, va target pane phai | User yeu cau giao dien man nay phai gan nhu 1:1 voi anh/file HTML da gui, khong chi dung cung visual language. | Screen exception nay duoc nang cap: khong dung KPI strip chuan, thay vao do la top stat chip compact + workspace `Danh sÃ¡ch BOT / GÃ¡n cho ngÆ°á»i nháº­n`, trong khi backend submit contract van giu nguyen. | 2026-03-31 |
| Panel Cap phat BOT se luon hien va luu theo desired state cua target da chon | User muon nhin ngay BOT hien co cua manager/user, them bot tren cung mot panel, roi bam Luu cap phat de commit mot lan thay vi thao tac tung BOT roi submit ngay. | UI cap phat phai preload BOT hien co, cho phep thu hoi bang x, backend reconcile theo toan bo worker_ids gui len, va submit rong phai hop le de clear assignment. | 2026-04-01 |
Purge channels when BOT owner changes | Khi go/doi BOT cua user hoac manager, toan bo channel/job tren BOT cu phai bi xoa de tranh mang theo du lieu cua owner cu | Cac flow update_bot, reconcile_assignment_target_bots va delete_bot tro thanh destructive voi channel tren BOT cu | 2026-04-01
| Browser upload se ket thuc ngay o buoc `Chi tiet` khi YouTube da nhan file va chuyen sang xu ly/draft | User chi can upload video vao draft, khong can bot di het wizard `Next/Done`; flow cu dai va de treo o cuoi. | Worker se theo doi progress/status o footer dialog upload, xac nhan sau 100% khi YouTube chuyen sang processing/da nhan file, roi bam `X` dong dialog va de video o draft. | 2026-04-01 |
| Browser uploader phai fail som khi Chrome profile bi redirect sang Google Sign in hoac verification challenge | Cac job browser upload dang dung o 1% nhung thuc te khong vao duoc upload dialog; debug artifact cho thay trang dang la Google Sign in. | Worker se tra loi ro rang `Chrome profile cua kenh da mat dang nhap...` hoac `Google dang yeu cau xac minh...`, giup tranh timeout gia va chi ra can reconnect profile. | 2026-04-01 |
| Khi doi chieu launch saved profile, uu tien coi redirect `Google Sign in` la van de session/profile tru khi ca ban launch cu va moi cho ket qua khac nhau | Test truc tiep tren `worker-02` bang ca launch cu (truoc khi doi flow upload) va launch moi deu cung redirect sang `Google Sign in`; inspect profile tren disk cung cho thay `account_info = None`, `signin.allowed = False`. | Khong tiep tuc quy loi cho draft flow/footer tracking o nhung case nay; can recheck mapping profile hoac reconnect kenh tren VPS truoc khi test tiep luong upload moi. | 2026-04-01 |
| Browser profile cleanup production chi giu profile dang duoc channel tham chieu, bo qua `browser_sessions` da `closed` | Runtime cho thay profile cu tich tu theo moi lan reconnect/xoa kenh, de lai nhieu thu muc stale va de gay nham khi debug; cac session cu deu da dong va khong con la source of truth. | Worker-01/worker-02 chi giu lai profile key dang gan voi channel live; viec dieu tra tiep phai bam theo current `browser_profile_key` thay vi profile cu ton dong tren disk. | 2026-04-01 |
| Xoa kenh phai queue cleanup browser profile xuong worker va khong co ky vong tai su dung profile cu | User muon xoa kenh la xoa sach session Chromium tren VPS; profile stale de lai khong duoc map lai tu dong vao lan reconnect va chi lam rac runtime/nham debug. | Worker API poll/ack phai day du `cleanup_profiles`; sau khi xoa kenh, profile key cu se bi worker xoa tren disk va lan ket noi lai sau do se tao profile moi. | 2026-04-01 |
| User render form tren workspace se dung layout 2 cot `Render Config 3/5 + My Channel 2/5`, khong con cot `Quick Settings` | User yeu cau man tao job bam sat mockup moi va file `final_user_ui - Copy.html`, trong do title/lich/render nam cung panel cau hinh thay vi tach thanh cot giua. | User dashboard se bo `Link Intro`, `Link Outro`, `Mô tả`, counter `0/100`; frontend form/JS chi con xu ly `Tên video`, `Link video loop`, `Link audio loop`, `Thời lượng render`, `Hẹn lịch đăng`. | 2026-04-01 |
| User workspace ho tro `1 user -> nhieu VPS`, va flow `+ Them Kenh` phai cho chon VPS neu user co tu 2 may tro len | User muon giu kenh/browser session theo tung VPS duoc cap rieng, nhung van giu thao tac them kenh gon va dong bo voi visual workspace hien tai. | Store/API/user dashboard phai tra danh sach VPS duoc cap, browser session API nhan `worker_id`, popup grid 2 cot cho phep click thang vao VPS de tao session/noVNC tren dung may do, con channel/job van bam theo worker cua chinh no. | 2026-04-02 |
| Admin assignment workspace phai bo hoan toan lock `1 user = 1 VPS` va luu/replay day du danh sach `worker_ids` cua user | User da xac nhan nhu cau cap nhieu VPS cho 1 user khong chi o user workspace ma ca tu luong cap phat admin. | `reconcile_assignment_target_bots`, `update_bot`, `add_user_bot`, admin context va man `Cấp phát BOT / BOT của user / Danh sách VPS của user` phai ho tro them-bot, thu-hoi-bot va hien thi nhieu VPS nhat quan. | 2026-04-02 |
| User render form giu `Link Intro` o che do tuy chon, hidden-by-default trong footer trai thay vi dua lai thanh field co dinh | User muon goi lai intro khi can nhung khong muon form render mat do cao thuong xuyen hay quay lai layout cu. | `Render Config` se them toggle/panel nho o khoang trong ben trai footer; chi khi bat moi hien input `Link Intro` + upload local, reset/submit bo qua hoan toan intro neu panel dang tat. | 2026-04-02 |
| User render form dat action `+ Them Intro / - Bo Intro` o header `Render Config`, va field intro phai chen giua `Ten video` voi `Link video loop` | User muon layout bam sat file mockup/reference moi hon va giu footer action gọn lại nhu truoc. | Header `Render Config` phai co text action cung pattern voi `+ Them Kenh`; intro section animate mo/thu giua form, va tat intro phai xoa toan bo link/file/upload state cua slot nay. | 2026-04-02 |
| Cac control chinh trong `Render Config` phai giu cung nhip chieu cao va intro section an khong duoc de lai spacing trong `space-y-5` | User yeu cau form bam sat mockup tham chieu, trong do title/input upload/time/date can dong deu va intro an di khong lam lech layout. | `panel-input` se dung cao 42px dong bo voi dropdown/upload rows; `introSlotPanel` phai toggle class `!mt-0` khi an de khong an khoang cach giua `Tên video` va `Link video loop`. | 2026-04-02 |
| Nút upload local trong user render form phai do mot listener duy nhat xu ly ca `mo file picker` va `clear upload state` | Local browser file picker dang khong mo on dinh khi tren cung mot nut co hai click listener tach roi (mot cai mo input, mot cai clear state), de gay bug kho lap lai tuy browser/cache. | `data-upload-trigger` se tu xu ly: state `idle` => `input.click()`, state khac => clear upload; template phai bump query string JS moi khi sua flow nay de tranh cache client. | 2026-04-02 |
| User render form phai uu tien giu nguyen upload UX cu: chon file la upload ngay, co progress ring, trang thai `Dang chuan bi upload / % / Hoan tat`, va nut cung luc dong vai tro `chon file` hoac `huy/xoa upload` | User muon hanh vi upload local giong ban truoc khi doi UI, khong chi mo picker ma con phai giu day du feedback trong qua trinh upload. | JS trigger upload se uu tien `showPicker()` neu browser ho tro, fallback `input.click()`, va khong duoc loai bo cac hook `setSlotStatus / setUploadVisual / clearSlotUpload / startSlotUpload` dang tao progress + success state cho tung slot. | 2026-04-02 |
| File picker local phai co fallback cho browser chặn `showPicker()`/`click()` tren `input.hidden` | Thuc te local browser van co the khong mo duoc picker du da dung mot listener duy nhat, do `showPicker()` nem loi hoac `input` dang `display:none`. | `openSlotFilePicker()` se thu `showPicker()`, neu fail thi tam bo class `hidden`, day input ra ngoai viewport, `click()`, roi restore style/class ngay sau do; moi fix browser-specific cho upload local phai bump cache key template. | 2026-04-02 |
| Modal browser session chi giu mot vung huong dan/xac nhan kenh, khong lap them card `Chua nhan dien duoc kenh` o phan duoi | User phan hoi panel nay lap lai y nghia voi phan trang thai/huong dan ben tren va lam modal bi dai khong can thiet. | Template/JS se bo `browserSessionDetectedChannel`; modal chi giu stats, panel huong dan, va error panel khi can. | 2026-04-02 |

| Notice trong admin/user workspace phai dong bo theo pattern co nut x + tu an sau 5s; role assignment admin va channel select user uu tien search ngay trong control hien co | User phan hoi thong bao dang bi treo co dinh, form gan role manager/admin tim mu, va chon kenh can co search nhanh khi so luong lon. | Admin notice partial + user dashboard notice/toast phai su dung transient behavior chung; form gan role manager/admin doi sang searchable select cua user that; channel select them search inline va My Channel gioi han scroll noi bo. | 2026-04-02 |
| Browser upload phai giu parity visibility voi app cu: `draft`, `private`, `unlisted`, `public` la cac mode hop le o ngay contract tao job | User van can luu nhap khi can, nhung luong publish phai giong app cu thay vi hardcode draft-flow; du lieu `visibility` da ton tai trong store/job nen can noi thong UI -> backend -> worker cho day du. | User dashboard gui `visibility`, `JobCreatePayload`/`RenderJobRecord` luu gia tri nay, va worker upload target tach ro `is_draft` voi `privacy_status` thay vi xem dialog draft la flow duy nhat. | 2026-04-04 |
| Browser uploader van dung dialog telemetry lam nguon truth cho progress, nhung nhánh publish phai di qua `Review -> visibility -> Done` | User can app van theo doi `upload_progress` va nhan biet upload thanh cong, nhung muon parity voi app cu khi chon private/unlisted/public. | Worker se tiep tuc doc `%/status` tu dialog de cap nhat progress; draft branch dong dialog sau khi transfer duoc commit, con publish branch phai click Review, set visibility, doi transfer xong roi bam Done truoc khi complete job. | 2026-04-04 |
| Completion message cua browser upload phai map theo visibility cuoi cung thay vi chi bao telemetry generic | User can biet app da luu video thanh cong o mode nao, nhat la sau nhieu lan sua flow upload/complete. | `BrowserUploadResult`/`complete_job` se mang thong diep `Đã lưu video ở chế độ ...` de UI va log phan biet duoc draft voi private/unlisted/public ma khong can match watch URL hay row content-list. | 2026-04-04 |
| Chuyen bootstrap memory cua project sang `PROJECT_BRIEF + MEMORY_INDEX + DECISIONS_INDEX`, giu `PROJECT_CONTEXT + DECISIONS + WORKLOG + CHANGELOG` lam lop lich su/tuong thich | Bo docs cu da phinh lon va dang an nhieu token moi khi bootstrap task; can giam context vao nhung file ngan hon ma khong lam gay workflow dang dung. | Task moi se start nhanh hon, docs cu van ton tai de doi chieu lich su, va root AGENTS co the duoc rut gon ma van giu tuong thich nguoc. | 2026-04-02 |
| Voi repo lon, uu tien `code-graph/cgraph` lam retrieval layer truoc khi mo nhieu file, nhung code va memory docs van la canonical source | Project co nhieu backend/template/worker/docs, grep tay va doc full history de bi ton token va cham. | Future tasks co the khoanh vung symbol/module/dependency nhanh hon, nhung moi quyet dinh van phai duoc xac minh lai bang code/docs chuan. | 2026-04-02 |
| `admin` va `manager` duoc vao chung workspace `Điều phối Render` qua `/app`; manager dung kho VPS cua minh, admin dung VPS tu tu cap phat cho chinh tai khoan admin | User muon hai role van hanh cung co the render nhu user ma khong tao them mot workspace tach biet, dong thoi admin can tu cap VPS cho chinh minh de thao tac. | Route `/app` se bridge `admin session -> app session` thay vi redirect ve admin shell; API `/api/user/*` va store workspace phai chap nhan role `user/manager/admin`; bot assignment phai xuat hien target admin-self trong luc cap phat. | 2026-04-03 |
| Một worker/VPS có thể được gán cho nhiều user; khi gỡ user khỏi worker chỉ được xóa dữ liệu của đúng user đó trên worker đó | User cần nhiều user cùng dùng chung một VPS nhưng dữ liệu channel/browser session/job phải tách theo từng user; bug cũ còn xóa sạch data khi đổi tên BOT hoặc gỡ một mapping. | `user_worker_links` phải cho phép nhiều cặp `(user_id, worker_id)`; cleanup đổi từ `worker-wide` sang `user+worker scoped`; đổi tên BOT không được đụng vào channel/link hiện hữu, còn xóa mapping phải chỉ gỡ channel grant/session của đúng user đó. | 2026-04-03 |
| Worker cleanup se tach thanh 3 lớp ro rang: per-job cleanup, browser-session/profile cleanup, va janitor startup/periodic cho artifact stale | Thuc te production cho thay file render/output cu co the con ton tai neu worker crash, job bi xoa o app sau khi worker khong kip chay `finally`, hoac de lai runtime tam browser-upload. | Van giu cleanup tuc thoi trong `job_runner.py` va `browser_sessions.py`, nhung bo sung janitor worker-side de xoa `job-*`, `browser-upload-runtime/*`, `outputs/*` stale va kill `Xvfb` mo coi; khong can them cleanup contract chong cheo tu control plane. | 2026-04-03 |
| Scheduler moi worker giu `1 active job` va fair-share queue theo user tren cung worker | User xac nhan RAM/disk cua VPS khong du cho render + Chromium song song; can chot worker luon chay tuan tu, nhung khi mot VPS phuc vu nhieu user thi khong de mot user chiem hang doi qua lau. | `claim_next_job()` phai van chan `active_jobs >= 1` tren moi worker, nhung thay vi FIFO thuan thi se round-robin theo owner cua job tren cung worker; worker main loop chi claim job moi sau khi `run_job()` return nen cleanup local cua job truoc da xong roi moi sang job ke. | 2026-04-04 |
| Worker resilience se dung retry/backoff cho network HTTP, heartbeat background de tu reconnect sau mat mang/restart, Telegram offline alert tri hoan 180s, va job `uploading` chi duoc `requeue` khi mat tracking luc upload moi bat dau | User chua can resume render/upload, nhung bat buoc worker khong chet vo co khi mang chap chon, phai tu dang ky lai voi control-plane sau reboot/restart, va admin can co canh bao Telegram that khi bot offline han. | `workers/agent/control_plane.py` retry cac call retryable; `workers/agent/main.py` giu heartbeat background + re-register khi worker record mat; `backend/app/store.py` co monitor thread + Telegram alert env-driven, va `_reconcile_worker_jobs_from_heartbeat()` tach `requeue` som voi `error` pha upload de tranh duplicate video. | 2026-04-04 |
| Job dang chay chi duoc chuyen `pending/error` sau khi vuot `lease/grace window`, khong phan ung ngay khi hut 1-2 heartbeat | User muon worker tiep tuc render/upload khi control-plane mat mang tam thoi, va control-plane phai cho worker co co hoi reconnect/retry truoc khi ket luan job bi gian doan. | `backend/app/store.py` doi lease thanh env `WORKER_JOB_LEASE_SECONDS`, heartbeat missing active job chi danh dau gian doan sau khi qua han lease, va monitor nen quet lease expired de render requeue / upload error mot cach co kiem soat. | 2026-04-04 |
| Them worker/BOT moi se di qua mot duong SSH bootstrap duy nhat tren control-plane; UI admin va CLI chi la hai mat goi chung mot helper, khong bootstrap qua Telegram | User chot bo huong Telegram vi rui ro secret/ops, dong thoi muon mot flow them BOT thuc dung co the dung tren UI va script ma khong sinh hai contract cai dat khac nhau. | `backend/app/worker_bootstrap.py` tro thanh helper canonical de SSH sang VPS, upload env + bootstrap scripts, bat `youtube-upload-worker.service`; `backend/app/templates/admin/worker_index.html` va `scripts/add_worker.py/.ps1` chi goi lai helper nay, con BOT ID se duoc cap so tu control-plane theo `worker-XX`. | 2026-04-04 |
| `Xoa BOT` phai la decommission that tren VPS va `Them BOT` phai hien row provisioning live ngay trong Danh sach BOT | User khong chap nhan xoa nửa vời vi worker co the tu register lai, dong thoi muon admin thay ngay tien trinh cai BOT ma khong phai reload/man hinh rieng. | Control-plane can luu connection profile toi thieu (`vps_ip`, `ssh_user`), queue install/decommission operation, overlay row BOT dang cai/dang go trong `Danh sach BOT`, va chi xoa state local sau khi SSH decommission tren VPS thanh cong. | 2026-04-05 |
| Bootstrap/decommission worker phai chiu duoc Windows line endings va viec tai tao worker tren cung VPS | Live smoke voi `109.123.233.131` lo ra 3 bug van hanh: `python3 -m venv .venv` va vao symlink ton tai khi reinstall tren cung VPS, script `.sh` upload tu Windows mang `CRLF` lam bash Linux fail, va failed install placeholder giu slot `worker-XX`; them nua, may worker trang khong boot duoc vi `requirements.txt` thieu `selenium`. | `backend/app/worker_bootstrap.py` phai normalize script upload sang LF, `scripts/bootstrap_worker.sh` tao venv trong runtime dir thay vi qua symlink, `scripts/decommission_worker.sh` xoa them leftover `.legacy/.clone`, `backend/app/store.py` khong de failed install task giu slot worker id, va `workers/agent/requirements.txt` phai ghi ro `selenium` de bootstrap tu GitHub tren may moi boot duoc ngay. | 2026-04-05 |
| Worker browser/upload live se tam thoi bam sat flow on dinh gan `012e614`: worker service chay duoi `root`, browser canonical la `chromium-browser`/`chromium`, va chi giu lai cleanup stale `Xvfb` + decommission cleanup de tranh state cu sot lai | Superseded sau khi audit live xac nhan `chromium-browser` tren Ubuntu worker thuc chat la snap wrapper va tiep tuc pha profile isolation khi nhieu account dung chung mot VPS. | Gia tri lich su van huu ich de doi chieu regression, nhung production hien tai khong con bam theo browser canonical nay nua. | 2026-04-06 |
| `admin` va `manager` duoc xem nhu account workspace rieng tren cung VPS, nhung `ChannelRecord` van global theo `channel_id` nen `confirm_browser_session` phai chan viec link them mot kenh da thuoc account khac | User muon nhieu account cung dung chung mot VPS ma khong lay kenh cua nhau; code hien tai da tach `user_worker_links` va `channel_user_links` theo account, nhung van reuse `ChannelRecord` toan cuc theo `channel_id`. | Giu mo hinh nhieu account chung worker thong qua link tables; `_normalize_visible_admin_relationships()` phai giu link cho `user/manager/admin`; workspace query phai doc theo link; va `_upsert_browser_connected_channel()` phai fail neu `channel_id` da linked sang account khac thay vi tu dong gan them vao account moi. | 2026-04-06 |
| Neu worker phai dung snap-backed `chromium-browser`, runtime phai override `SNAP_USER_COMMON`, `SNAP_USER_DATA`, `SNAP_REAL_HOME` va `XDG_*` theo tung `browser_profile_path`; bootstrap BOT moi van uu tien browser non-snap, nhung login va upload phai dung cung mot env builder de profile isolation hoat dong ca voi snap | Audit live tren `62.72.46.42` va `109.123.233.131` cho thay `chromium-browser` la snap wrapper va cookie/session that bi do vao `/root/snap/chromium/common/chromium`, lam nhieu account tren cung VPS de len nhau du app da co `browser_profile_key/path` rieng. Patch moi can danh trung goc benh thay vi tiep tuc debug quanh `ChannelRecord`/workspace links. | `workers/agent/browser_runtime.py` se detect snap va build env co `HOME`, `XDG_RUNTIME_DIR`, `XDG_CONFIG_HOME`, `XDG_CACHE_HOME`, `XDG_DATA_HOME`, `SNAP_USER_COMMON`, `SNAP_USER_DATA`, `SNAP_REAL_HOME` theo tung profile; `workers/agent/browser_uploader.py` buoc phai tai su dung cung env builder de upload mo lai dung kho cookie/session; `scripts/bootstrap_worker.sh` uu tien browser non-snap cho BOT moi nhung worker hien huu van co duong isolation an toan neu chua cai lai browser. Profile cu da bi o nhiem global state phai reconnect lai, khong co ky vong salvage. | 2026-04-06 |
| Production worker browser canonical chuyen sang native `google-chrome-stable`; login noVNC va upload van dung chung `profile_path + _build_browser_env()`, shutdown browser phai graceful de flush cookie, password store phai la `basic` tren worker headless, va moi profile/session browser phai co `HOME`, `DBUS`, `XDG_*` rieng de shared VPS khong de profile cua nhau | Audit tiep theo cho thay env-hack voi snap Chromium chi la giai phap tam, con `google-chrome-stable` la browser native ton trong `--user-data-dir` tot hon va khong anh huong toi noVNC, DevTools inspect, auto-detect channel hay attach-mode uploader. Sau khi doi sang Chrome Stable, van con hai lo hong can khoa lai: Chrome tren Linux headless co the rang cookie/profile vao OS keyring va lam profile cu nhin nhu da mat dang nhap, va nhieu account tren cung VPS co the van chia se mot phan state neu khong tach `DBUS`/`XDG` theo tung profile. `--password-store=basic` giai quyet benh keyring, con `_build_browser_env()` phai tiep tuc pin `HOME`, `DBUS_SESSION_BUS_ADDRESS`, `XDG_CONFIG_HOME`, `XDG_CACHE_HOME`, `XDG_DATA_HOME`, `XDG_RUNTIME_DIR` theo `profile_dir/session_dir` de moi account browser session tren cung worker co mot kho state rieng. Rui ro lon nhat con lai cua huong nay la lech `chromedriver` neu cai tay co dinh, nen can chuyen sang co che de Selenium Manager tu lay driver khop version runtime. | `workers/agent/browser_runtime.py` uu tien `google-chrome-stable/google-chrome`, khi launch phai them `--password-store=basic`, va `_build_browser_env()` phai tao env co `HOME=profile_dir`, `DBUS_SESSION_BUS_ADDRESS=/dev/null`, `XDG_CONFIG_HOME`, `XDG_CACHE_HOME`, `XDG_DATA_HOME` theo tung profile, `XDG_RUNTIME_DIR` theo tung session; `workers/agent/browser_uploader.py` launch upload cung phai dung y chang env builder do; `scripts/bootstrap_worker.sh` mac dinh env moi sang `google-chrome-stable` va cai browser native tu repo chinh thuc cua Google, worker live phai remove stale `chromedriver` binary thay vi pin mot version co dinh, `workers/agent/browser_runtime.py:stop()` giu `_graceful_shutdown_chromium()` de khi auto-confirm dong session thi browser co thoi gian flush cookie/profile xuong dia. Profile cu da duoc ma hoa/luu sai tu truoc do van can reconnect lai. | 2026-04-06 |
| Live stream dung chung account/role model voi upload, nhung tach rieng live bot pool, live assignment `primary/backup`, va CRUD route cua live stream | User xac nhan muc tieu la mot user/manager/admin duy nhat co the dung ca upload va live trong cung app, nhung hai domain van la hai luong van hanh rieng: upload can worker + channel assignment, live can bot chinh/backup va khong duoc tai su dung chung upload worker pool. Neu giu chung worker/assignment thi admin va manager se bi lan domain, cap nham bot, va form live co the luu sai owner/scope. | Canonical source hien nay la `users` dung chung cho ca upload va live, nhung live phai co `live_workers`, `live_user_worker_links`, va `live_streams` rieng trong `backend/app/store.py`. Moi live assignment phai mang `live_role` (`primary`/`backup`), live form phai validate theo dung role nay, va CRUD route cho luong live phai thong nhat qua `/admin/live/*` va `/app/live/*` thay vi tai su dung contract upload. | 2026-04-11 |
| Workspace live cua admin/manager bo han `Danh sách Kênh`; username/password van dung chung mot account voi upload, con Telegram live se xu ly rieng o app workspace sau | User xac nhan flow live stream khong can module channel/onboarding kenh nhu upload, nen viec giu `Danh sách Kênh` trong workspace live chi tao nham lan domain. Dong thoi thong tin tai khoan co ban (`username/password`) phai la mot nguon dung chung cho ca upload va live; chi nghiep vu Telegram moi co the tach theo workspace o phase sau. | Sidebar/tabs workspace live phai bo duong vao `Danh sách Kênh`, route `/admin/channel/index?workspace=live` phai redirect sang `/admin/live`, va moi flow edit credential trong live/admin-manager tiep tuc sua tren cung `UserRecord` hien co thay vi tao profile tai khoan live rieng. | 2026-04-11 |
| Runtime live stream bo hoan toan nhanh `1080/4K`; chat luong phu thuoc media dau vao, audio URL la tuy chon va neu vang thi giu audio goc cua video | User xac nhan livestream domain hien tai chi can day file/media vao ffmpeg RTMP, nen viec tach `1080`/`4K` nhu app cu khong con y nghia nghiep vu va de lam lech model cap bot/runtime. Doi chieu `StreamClientService` cua app cu cho thay `1080/4K` chi anh huong nhan/live type va bot assignment, con ffmpeg command van `-c copy` len RTMP. Cung doi chieu logic audio cho thay khi co `audio` URL rieng thi ffmpeg tach audio video (`-an` cho video, `-vn` cho sound, `-stream_loop -1` cho sound, `-shortest`), con khi khong co audio rieng thi luong live su dung audio embedded trong file video nhu hien co. | Form/create/runtime contract moi cua live chi con `BOT chính`, `BOT backup`, `backup delay`, `stream key`, `video_url`, `audio_url?`, `start/end time`; khong duoc dua `LiveType/1080/4K` tro lai o backend hay worker runtime. Worker live ve sau se coi `audio_url` la input tuy chon: co thi mux audio rieng, khong co thi pass-through audio goc cua media video. | 2026-04-11 |
| Runtime live phase 1 dung namespace `/api/live-workers/*` rieng va giu 4 pha `download -> pre-render -> wait -> RTMP stream`; worker entrypoint phai lazy-import theo `runtime_mode` | User yeu cau bam sat runtime on dinh cua app cu nhung dua vao framework control-plane/worker hien tai. Doi chieu `WorkQueue.cs` cho thay luong cu thuc chat chay 4 pha ro rang; neu tiep tuc import cung luc upload stack trong worker entrypoint thi live mode se bi phu thuoc `selenium/browser upload` du domain da tach rieng. | Backend phai co contract `register/heartbeat/claim/progress/complete/fail` rieng duoi `/api/live-workers/*`, `backend/app/store.py` phai quan ly lease/status live doc lap, `workers/agent/live_runner.py` phai mo phong dung 4 pha cu, va `workers/agent/main.py` phai branch lazy import theo `WORKER_RUNTIME_MODE` de live worker co the chay du khong co upload/browser dependency trong env. | 2026-04-11 |
| Backup policy cua live phai bam sat app cu: luong co `EndTimeLive` start backup song song ngay tu dau tren backup ingest, con luong `24/7` chi failover sang backup sau khi primary disconnected qua `backup delay`, va backup 24/7 phai bi huy neu primary hoi phuc | Doi chieu `StreamClientService.cs` cho thay nhanh co `EndTimeLive != null` clone va `SendDataToBOTAsync()` cho backup ngay trong flow start, con `CheckLiveBackupAsync()` chi chay voi `EndTimeLive == null` va chi spawn backup sau khi `DisconnectedTime` vuot `TimeBackup`. Dong thoi nhanh `RenderStatus.LiveStreaming` cua primary chi huy backup khi `EndTimeLive == null`, tuc la nhanh 24/7 moi co delayed failover + auto cancel backup upon recovery. | Runtime moi khong nen coi backup la upload-style requeue. Can dung lease/heartbeat model cua framework hien tai de detect mat ket noi, nhung chinh sach kich hoat backup phai phan nhanh: finite scheduled live => start primary + backup song song; 24/7 => start backup tre sau `backup_delay_minutes`, va cancel backup neu primary hoi phuc. | 2026-04-11 |
| Route canon cua live app la `/app/live` cho moi role; `admin/manager` dung live app bang chinh account cua minh, con `/admin/live` chi giu de redirect tuong thich va cac man quan tri live van o cum `/admin/*?workspace=live` | Upload workspace da co pattern `admin/manager` vao chung `/app` bang session bridge, trong khi live dang bi tach thanh `/app/live` (user shell) va `/admin/live` (admin shell), lam admin/manager khong van hanh live app trong chinh account cua minh va tao cam giac ton tai hai app. Scope quan tri rong hon cua live van can giu o cac man `/admin/user/index?workspace=live`, `/admin/ManagerBOT/index?workspace=live`, `/admin/render/index?workspace=live`; chi route dieu phoi live moi can thong nhat nhu upload. | `require_app_access` phai bridge duoc `admin_auth -> AppSessionUser`, `GET /app/live` phai render cung pattern voi `/app`, `get_user_live_workspace_view()` phai tu bat `admin_shell` cho role `admin/manager`, nav `Điều phối Live Stream` cua admin phai tro ve `/app/live`, va `/admin/live` chi con redirect ve route canon moi. | 2026-04-12 |
| BOT live phai di qua cung SSH bootstrap/decommission pipeline voi upload va chon runtime bang `WORKER_RUNTIME_MODE=live`; khong cho local-create BOT live trong control-plane | Sau khi hop nhat live vao app hien tai, live van con mot nhanh tam thoi tao/xoa BOT chi o control-plane ma khong SSH cai dat worker that, dan den admin nhin thay BOT live trong UI nhung VPS khong co service live runtime de stream that. Cung luc do, neu khong khoa toan bo `Danh sách Kênh` trong `workspace=live`, flow upload channel cu van ro ri sang live va gay nham domain. | Live BOT install/delete phai goi chung `backend/app/worker_bootstrap.py`, store phai luu credential/task theo `workspace_mode`, script bootstrap phai ghi `WORKER_RUNTIME_MODE=live`, worker register/decommission phai dung dung pool live, va moi route/API `channel` trong live phai redirect/tra loi loi ro rang thay vi render module upload. | 2026-04-12 |
| Live worker bootstrap phai toi gian theo runtime live: tat browser session va YouTube upload flags de khong cai Chrome/noVNC stack cua upload tren VPS live | Smoke test tren `154.26.159.8` cho thay live BOT moi van dung env bootstrap cua upload, dan den VPS phai cai them `xvfb`, `openbox`, `x11vnc`, `websockify`, `novnc`, va `google-chrome-stable` du live runtime hien tai chi can `ffmpeg` + worker core. Hieu ung phu la add-bot live bi treo lau o pha `awaiting_registration`, mat khoang 2 phut moi thay BOT online du branch/app-dir da dung. | `backend/app/worker_bootstrap.py::_build_worker_env_file()` phai branch theo `runtime_mode`: voi `live` thi ghi `WORKER_UPLOAD_TO_YOUTUBE=false` va `BROWSER_SESSION_ENABLED=0`; `scripts/bootstrap_worker.sh` da co guard theo `BROWSER_SESSION_ENABLED`, nen live VPS se bo qua toan bo browser stack. Kiem chung tren preview `https://live.jazzrelaxation.com` + worker `154.26.159.8`: worker moi `live-worker-04` bootstrap xong va register online trong ~35s, thay vi nhieu phut nhu truoc. | 2026-04-12 |
