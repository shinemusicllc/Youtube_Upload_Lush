| Decision | Reason | Impact | Date |
| --- | --- | --- | --- |
| Rebuild UI theo huong HTML/template thay vi React/Vite | User danh gia React/Vite cu cung va khong dat chat luong visual mong muon. | UI moi uu tien FastAPI templates/static assets, tan dung HTML/CSS co san. | 2026-03-25 |
| Su dung `BaseSource.AppUI` va `final_user_ui.html` lam nguon UI chinh | Da co giao dien san, tiet kiem thoi gian va bam sat workflow app cu. | Can map lai Razor/HTML thanh templates va noi API FastAPI vao. | 2026-03-25 |
| Backend van chot FastAPI + Python worker + Postgres + Redis | User xac nhan giu kien truc backend/worker/deploy da thong nhat. | Can rebuild backend sach tu dau, nhung contract phai phu hop worker VPS model. | 2026-03-25 |
| User page render qua `backend/app/routers/web.py` voi seed context in-memory | User yeu cau giu `main.py` va cac router API khac khong doi, chi tao template co the render an toan tu UI moi. | User dashboard co the render ngay bang FastAPI/Jinja, va sau nay co the thay seed bang store/DB ma khong can doi layout. | 2026-03-25 |
| Dung seed contract in-memory JSON-friendly cho `user`, `worker`, `channel`, `channel grant`, `render job`, `oauth summary` | Can bootstrap nhanh backend khi chua co DB/persistence layer. | `backend/app/schemas.py`, `backend/app/store.py`, `backend/app/routers/*` se lam source of truth tam thoi cho shell API. | 2026-03-25 |
| Chot `user_dashboard.html` + `user_dashboard.js` lam shell user duy nhat, bo template user du thua | Can mot nguon UI duy nhat de noi backend va khong quay lai React/Vite. | `/app` va `/` bam theo HTML/CSS da chot; JS chi lam adapter cho form/job action. | 2026-03-25 |
| Admin FastAPI phai bam shell Elevated SaaS cua `BaseSource.AppUI` thay vi giu dashboard bootstrap tam | User da nang cap toan bo UI admin trong repo .NET cu va yeu cau noi backend vao dung giao dien nay. | FastAPI admin duoc tach thanh 4 route `user/bot/channel/render`, mount lai asset `admin-themes` + `css`, va `/admin` redirect ve `/admin/user/index`. | 2026-03-25 |
| `final_user_ui.html` la visual source of truth cho toan bo du an | User xac nhan can thong nhat visual language va khong muon admin tiep tuc bam style Elevated cua app cu. | Moi tai lieu UI va cac man hinh admin/user ve sau phai tham chieu `final_user_ui.html`; repo .NET cu chi con vai tro tham chieu luong nghiep vu. | 2026-03-25 |
| UI dung `BOT` thay cho `worker` trong tat ca nhan hien thi | User muon ngôn nguoi nhat quan giua admin va user, trong khi backend model worker van can giu nguyen de khong vo contract. | Cac template/JS se hien thi `BOT`, con schema/store/route backend van luu ten `worker` o cap du lieu. | 2026-03-25 |
| Admin UI giu workflow cu nhung gom du lieu vao cot thong tin tong hop thay vi trai raw field thanh qua nhieu cot | Admin can de them CRUD/parity logic sau nay ma khong quay lai layout roi, kho scan. | Cac man `User / BOT / Channel / Render` uu tien nhom metadata theo hang, chip trang thai va action compact, nhung khong doi route hay contract du lieu. | 2026-03-25 |
| User Management parity se bam sat app cu cho muc 1.1-1.7, con muc 1.8 dung page mapping BOT rieng tren FastAPI | Snapshot .NET cu cho `managerbot` khong nhat quan giua controller/view/service; can chot mot luong ro rang de tiep tuc parity ma khong treo o artifact do dang. | Module user co flow CRUD va mapping BOT dung duoc ngay tren local; cac module 2-5 co the tiep tuc tren cung visual/system contract nay. | 2026-03-25 |
| BOT relation pages trong FastAPI bam direct user-BOT mapping | Snapshot .NET cu suy dien user/BOT qua channel va controller khong nhat quan, trong khi module 1 da chot flow mapping BOT rieng. | Module 2.5 va 2.6 co page va action ro rang, thong nhat voi contract hien tai va de noi module channel ve sau. | 2026-03-25 |
| Channel Management dung mapping `channel_user_links` rieng thay vi suy dien tu BOT | Luong cap quyen kenh cua app cu co page va action rieng (`channel/user`, `channel/users`, `updateuserchannel`, `adduser`), nen suy dien tu BOT se lam sai nghiep vu. | Module 3 co the add/remove user tren kenh doc lap, DS User cua kenh dung du lieu that, va action xoa BOT khong con de lai mapping kenh rac. | 2026-03-25 |
| Render detail trong FastAPI giu kieu readonly config-first | App cu hien man `renderinfo` de doc lai cau hinh input, khong phai runtime diagnostics. | Module 4 chi hien Intro/VideoLoop/AudioLoop/Outro, channel, time render, schedule va title; khong mo rong thanh man debug khac he. | 2026-03-25 |
| Shared admin auth dung session-cookie va role gate theo route | Can dat parity voi app cu (`Admin`, `Manager`) nhung khong muon refactor vo lon toan bo router/template shell. | Admin web/API duoc bao ve bang session that, manager bi gioi han scope, va login/logout co the chay local ngay. | 2026-03-25 |
| Persistence tam thoi dung SQLite JSON snapshot truoc khi chuyen sang Postgres + Redis | Muc tieu hien tai la lam cho local bootstrap/admin prototype khong mat du lieu sau restart ma khong rewrite toan bo repository sang relational layer ngay. | State admin/user/channel/render duoc luu that o local, module 5 hoan tat, nhung van can migration plan khi chuyen production data layer. | 2026-03-25 |
| Google OAuth local bootstrap luu `refresh token` tren `ChannelRecord` trong SQLite snapshot va tu nap `.env` tai root workspace | Can kich hoat flow connect that ngay trong app hien tai ma chua co secret manager/DB production. | User co the connect YouTube channel that tren local bang `.env`; doi lai token hien dang duoc giu o local bootstrap layer va can migration/encryption truoc khi dua production. | 2026-03-25 |
| Worker media moi phai giu fast-path `copy/remux` cho case loop hop le, chi normalize/re-encode khi that su can | Trace app cu cho thay gia tri lon nhat cua pipeline hien tai nam o viec ghep loop bang `FFProbe + ffmpeg -c copy`, giup job 4K dai van kha nhe neu input dong nhat. | Ban rebuild can uu tien detect input "compatible" de di fast-path, dong thoi tach rieng fallback normalize/re-encode cho case sai fps/resolution/codec. | 2026-03-25 |
| Local upload cho file lon se di qua `upload session + resumable chunk upload`, job creation chi nhan asset refs da upload xong | `POST /user/jobs` kieu multipart don khoi khong chiu duoc file lon va mang chap chon, trong khi mo hinh host + worker can asset lifecycle ro rang. | Frontend phai upload chunk truoc, backend phai co session/asset layer, va ve sau co the doi storage backend ma khong vo contract tao job. | 2026-03-25 |
| Worker moi phai la agent outbound co `register + heartbeat` toi control plane, khong push job qua connection state nhu app .NET cu | Muc tieu mo rong 2 -> 50 worker can mo hinh pull/heartbeat on dinh hon push SignalR gắn chặt connection. | Da co scaffold `api/workers/*` va `workers/agent/main.py`; ve sau se mo rong them claim/progress/complete tren cung contract nay. | 2026-03-25 |
| Worker process that se duoc mo khoa theo 2 lop: contract `claim/progress/complete/fail` truoc, render/upload media that sau | Host shared cua user can uu tien route/job orchestration an toan truoc khi bat worker dong vao job that; dong thoi can co `simulate mode` de dien tap khong pha data. | Da co `simulate mode` opt-in va worker se khong tu y nhan job that tren VPS cho den khi chu dong bat env va noi FFmpeg/YouTube logic. | 2026-03-26 |
| Domain cho app host shared se di qua Caddy chung hien co va reverse proxy sang app Python o `172.17.0.1:8000` | Host da co nhieu app va mot Caddy container dang nam doc quyen `80/443`; them site block moi vao stack chung an toan hon mo them reverse proxy rieng. | Server-side route cho `ytb.jazzrelaxation.com` da duoc gan ma khong dung vao app khac; ban than HTTPS van phu thuoc DNS public tro dung ve host. | 2026-03-26 |
| Recovery sync tu VPS chi dong bo source dang chay, khong mirror runtime artifact hay repo-only file | Thu muc /opt/youtube-upload-lush la deploy copy khong co .git, co the thieu file repo-only va chua runtime artifact/backups; mirror thang se lam sach sai repo. | Khi can lay code nguoc tu VPS, chi sync backend/workers/infra/scripts/docs va root source can thiet, loai YoutubeBOTUpload-master, .env*, .venv, data, worker-data, .backup, .bak*. | 2026-03-30 |
| Deploy production se chay theo mo hinh git-first checkout, runtime tach rieng bang symlink | Thu muc /opt/youtube-upload-lush da bi sua nong va copy tay qua nhieu lan, gay lech giua local/GitHub/VPS va kho rollback. | Control-plane/worker se clone reset tu origin/main, con .env/.venv/backend-data/worker-data/.backup song o /opt/youtube-upload-lush-runtime; future deploy chi con bootstrap script + restart service. | 2026-03-30 |
| User workspace uu tien flow `browser session + noVNC` de them kenh, con OAuth giu lai lam fallback | Bai toan quota/publish verification khien user muon tu dang nhap Chromium tren Ubuntu thay vi redirect sang Google OAuth tren browser local. | Backend phai quan ly session Chromium/noVNC, channel record can luu them metadata `browser_profile`, va deploy host phai co GUI/runtime packages tren Ubuntu. | 2026-03-30 |
| Browser session phai chay tren worker/VPS duoc gan cho user, khong chay tren control plane | User se render va upload tren cung mot VPS; copy profile Chromium giua cac VPS sau khi dang nhap la kem ben va ton thoi gian. | Control plane chi giu session request + state; worker agent phai poll/sync browser session, tu launch Chromium/noVNC tren VPS cua no, va user dashboard mo link noVNC cua chinh worker do. | 2026-03-30 |
| Moi user chi duoc gan 1 worker/VPS tai mot thoi diem, va kenh/job chi hop le tren worker do | User xac nhan render va upload phai cung nam tren 1 VPS vi file render rat lon; worker khac khong nen render/upload thay. | `user_worker_links` phai duoc chuan hoa thanh mapping 1-1, dashboard user chi hien kenh/job cua worker duoc cap, va neu doi VPS thi user can reconnect kenh tren VPS moi truoc khi tao job tiep. | 2026-03-30 |
| Moi lan `+ Them Kenh` phai mo 1 Chromium/profile rieng cho tai khoan/channel do, dong thoi van auto-pass noVNC password qua URL fragment | User can tach biet tung tai khoan YouTube tren cung 1 VPS, giu session dang nhap rieng tung kenh, va khong muon user cuoi phai go VNC password tay moi khi mo noVNC. | Browser session moi khong reuse profile theo `user + worker` nua; xoa kenh/reconnect profile phai queue cleanup de worker xoa dung Chromium profile va job lien quan. | 2026-03-30 |
| Nut `+ Them Kenh` phai luon tao session moi va modal chi mo nut dang nhap khi worker bao san sang | User bi nham voi session cu/chua san sang, dan den mo nham browser cu va bam noVNC lien tuc khi worker chua launch xong. | Backend se dong session active cu truoc khi tao session moi; UI modal phai co trang thai `dang khoi tao` ro rang, an nut `Dang nhap kenh` cho toi khi noVNC that su san sang. | 2026-03-30 |
| Kenh `browser_profile` se upload bang `Python + Selenium + Google Chrome stable` tren chinh worker thay vi YouTube API | User muon render va upload tren cung 1 VPS, tranh quota API, va tan dung session Chrome da dang nhap khi add kenh qua noVNC. | Worker phai lay `browser_profile_key/path` tu control plane, mo Chrome profile tren worker de vao `YouTube Studio upload`, va cleanup `output/downloads` sau khi upload xong. | 2026-03-30 |
| Luong `OAuth/API upload` duoc neo lai tren branch backup `codex/oauth-api-backup` tu commit `3ba7287`, con huong chinh se uu tien `browser_profile only` | User muon on dinh hoa mot luong duy nhat cho production nhung van can duong rollback nhanh neu quay lai quota/API sau nay. | Co the tiep tuc loai bo OAuth khoi nhanh chinh ma khong mat lich su/implementation cu; khi can co the checkout/pick tu branch backup. | 2026-03-30 |
| Nhanh hien tai chot `browser_profile only` cho user workspace; route/callback OAuth tren web/API bi vo hieu hoa | User muon duy tri mot luong production on dinh, giam complexity, va da luu luong OAuth/API upload tren branch backup rieng. | UI chi mo `+ Them Kenh` -> Ubuntu Browser; worker upload chi di browser session, con channel oauth cu neu co se bi yeu cau reconnect bang browser. | 2026-03-30 |
| Browser channel metadata se duoc self-heal luc doc dashboard/bootstrap neu state dang luu ten generic hoac thieu avatar | Production co the ton tai channel browser duoc tao truoc patch enrich metadata, dan den UI hien `Trang tong quan cua kenh` va avatar fallback. | Chi can refresh dashboard la backend tu backfill ten/avatar dung cho channel va cac job lien quan, khong can script tay moi lan. | 2026-03-30 |
| Browser upload chi duoc complete sau khi worker thay upload transfer dat 100% va khong xoa output som khi chua co watch URL | Production cho thay Selenium dang complete ngay sau nut `Done`, lam browser dong som, output bi xoa som va job co the treo upload tren YouTube. | Worker browser upload phai doi tien trinh transfer that, UI completed phai hien upload 100%, va output duoc giu lai an toan hon neu chua co watch URL. | 2026-03-30 |
| Modal them kenh se huong dan 2 buoc ngan gon va auto-confirm/auto-close khi worker da nhan dien dung channel | User muon flow them kenh gan voi trai nghiem OAuth hon, giam thao tac tay va giam nham lan giua noVNC/browser/session. | User chi can bam Dang nhap Google roi vao dung YouTube Studio; app tu xac nhan, dong session va quay lai workspace, con nut xac nhan thu cong chi la fallback. | 2026-03-30 |
| Progress `download/upload` tren worker phai bam theo ratio thuc te cua transfer thay vi checkpoint noi suy | So sanh voi nhanh `codex/oauth-api-backup` va trace production cho thay upload browser dang bi nen vao vung `95-99%`, con download da co callback theo byte nhung `job_runner` khong dung den. | `download` se gom ratio cua tung asset vao tien trinh chung; `browser upload` se doi transfer 100% truoc nut `Done` va dong bo bar app theo % ma YouTube/stream tra ve. | 2026-03-31 |
| Worker phai co runtime cleanup sweep dinh ky de don job-* va outputs/job-* qua han | Cleanup ngay sau upload co the van sot file mo coi khi worker/job/UI lech pha hoac process bi ngat bat thuong. | Mac dinh worker se quet moi 15 phut, xoa job-* va outputs/job-* cu hon 6 gio, va bo qua khi WORKER_KEEP_JOB_DIRS=true. | 2026-03-31 |
| Browser session modal tren user workspace phai toi gian hoa va chi giu thong tin can hanh dong | User da khong con can doc nhieu text huong dan vi flow them kenh hien tai auto-confirm/auto-close. | Modal them kenh se bo huong dan dai, bo panel nhac lai, giu nut Dang nhap Google luon hien nhung disabled cho toi khi session san sang. | 2026-03-31 |
| Session Chrome cho browser onboarding se suppress prompt Chrome sign-in/sync thay vi co gang auto-enroll Chrome profile | User chi can luu website session Google/YouTube; prompt Chrome profile/sync de gay roi va khong can thiet cho use case nay. | Browser runtime se tiep tuc luu user-data-dir cho tung kenh nhung tat password manager/profile picker/signin intercept/sync prompt de giam thao tac thua. | 2026-03-31 |
| Browser upload failure phai surfacing error message ro rang tren dashboard | Browser upload co the fail vi verify/limit cua YouTube hoac Selenium tra ve chuoi rong `Message:`; neu chi hien badge `Loi` thi user khong biet cach xu ly. | Worker se format loi co nghia hon va dashboard row se hien `error_message` truc tiep de user nhin ra nguyen nhan ngay. | 2026-03-31 |
| Browser upload chi duoc complete khi YouTube khong con hien `Huy tai len`, va output fail cung phai duoc don ngay | Production cho thay app co the complete som khi row video xuat hien trong Studio du YouTube van giu trang thai upload 100% va nut huy, dong thoi output fail de lai file mo coi. | Worker se tiep tuc doi YouTube hoan tat upload truoc khi complete/cleanup thanh cong; neu upload fail thi `final output` cung bi xoa ngay thay vi de sweep dinh ky. | 2026-03-31 |
| Browser upload chi duoc complete khi row cua chinh video chuyen sang uploaded state, khong chi vi row xuat hien | Trace job dai cho thay YouTube co the da tao row trong Studio nhung van con `Dang cho xu ly` va nut `Huy tai len`; chi dua vao su ton tai cua row se lam app complete som va xoa output. | Worker se doi row title trung khop vao trang thai uploaded that tren content list; job cu complete som khong the tua nguoc, nhung job moi se khong xoa output khi YouTube van dang finalize. | 2026-03-31 |
| Browser upload se gan marker duy nhat theo `job_id` vao description de doi chieu dung row khi co nhieu upload song song | Neu chi match theo title hoac status global, app co the complete/xoa file nham khi co nhieu video dang upload hoac title trung nhau. | Worker se append marker vao description upload, mo dung edit page cua row candidate tren Studio de tim marker, va chi complete job khi row chua marker do vao uploaded state. | 2026-03-31 |
| Admin BOT manager-user-channel state se duoc chuan hoa theo cac mapping dang hien thi tren UI | Production da co state lech manager/channel (`manager-alpha`) va modal BOT khong phan anh dung owner hien tai cua VPS. | Restart control plane se tu dong dong bo `worker.manager_*`, `channel.manager_name`, `job.manager_name` va loc bo link duplicate/khong hop le de admin UI nhin mot nguon su that nhat quan. | 2026-03-31 |
| Worker production mac dinh phai khoa ve `1 VPS = 1 job active` va chan duplicate worker process tren cung may | Job nhe dang upload va job nang dang render dong thoi tren cung worker cho thay VPS bi overcommit; cleanup sweep 15 phut khong phu hop vi nguong xoa thuc te la 6 gio. | Worker config se clamp `capacity/threads = 1` khi `WORKER_SINGLE_JOB_ONLY=true` (mac dinh), va agent se giu lock file de tu choi process worker thu hai tren cung VPS. | 2026-03-31 |
| Lease/heartbeat cua worker chi duoc gia han dung job_id dang bao progress, heartbeat idle phai gui `active_job_ids=[]` | Live production xuat hien job ma do backend gia han lease cho toan bo job cung worker_id, khien job cu khong het han va worker tiep tuc bi xem la dang xu ly song song. | Control plane se tu nha/fail claim treo dung job, job dang bao progress khong con vo tinh nuoi lease cho job khac, va restart worker se phuc hoi state sach hon. | 2026-03-31 |
| Admin UI chi duoc hien thi luong BOT o che do read-only trong khi production khoa 1 VPS = 1 job active | Neu van giu nut/form sua luong trong admin thi van gay hieu nham rang VPS co the chay song song, du backend/worker da khoa ve 1. | Man admin BOT va user-VPS se chi hien 1/1 hoac note co dinh; route/API neu bi goi truc tiep cung chi tra ve `thread = 1`. | 2026-03-31 |
| Cap phat BOT duoc gom ve tab `Danh sach BOT`, trang BOT trong user chi con che do thong tin/shortcut | User muon thao tac cap, doi, thu hoi BOT theo loat va nhin duoc toan canh BOT trong cung mot man hinh; panel cap BOT o tung user gay cham va de lech live JS/HTML. | Admin va manager co mot diem dieu phoi duy nhat de cap BOT, live table chi con mot nguon su that, va trang user khong con chua logic cap phat truc tiep. | 2026-03-31 |
| Browser upload duoc xem la hoan tat khi YouTube da nhan xong file va row dung marker co watch URL on dinh, khong can doi transcode xong | Video dai co the mat rat lau o pha processing/checking sau khi transfer dat 100%; neu worker doi row sang uploaded hoan toan moi complete thi se fail oan sau 1 gio va xoa output. | Worker se complete ngay khi YouTube da nhan file that su, row cua job da xuat hien dung marker, va khong con nut huy upload; nhieu video dai se khong con mac timeout finalize gia. | 2026-03-31 |
| Browser upload duoc complete ngay khi row dung marker da co watch URL va YouTube khong con hien Huy tai len, du row van dang processing | Trace live cho thay YouTube row da xuat hien va cancel-upload da bien mat nhung worker van lap o 99% vi con doi them co hieu noi bo 	ransfer_completed. | Job app se chuyen sang completed sat hon voi trang thai that cua Studio; phan transcode/processing ve sau thuoc YouTube, khong nen giu app o dang upload. | 2026-03-31 || Browser uploader phai mirror milestone complete cua nhanh OAuth API cu: YouTube da nhan file va da cap video/watch identity, khong doi processing/transcode xong | Nhanh codex/oauth-api-backup complete ngay khi resumable upload tra 200/201 va ideo_id, nen video dai khong bi treo o 100% du YouTube van xu ly them. | Browser flow duoc phep chot completed ngay khi row/watch URL on dinh va Huy tai len bien mat; neu tiep tuc doi processing xong thi se lap lai bug treo dang upload voi video dai. | 2026-03-31 || Browser uploader phai detect title/description editor theo upload dialog semantics, khong duoc phu thuoc duy nhat vao id=textbox | YouTube Studio co the render editor bang DOM khac nhau theo rollout/UI state; selector cu gay TimeoutException o upload 1% du upload flow da vao dung trang. | Worker se robust hon truoc thay doi giao dien Studio va giam cac loi fail som truoc khi den bug finalize 99-100 phan tram. | 2026-03-31 || Browser upload phai quet editor xuyen `shadowRoot` va dump debug artifact khi van timeout o buoc metadata | YouTube Studio rollout moi van co the dat title/description editor sau custom elements/shadow DOM, khien selector light-DOM fail o upload 1% va khong de lai dau vet de trace. | Worker se thu tim editor sau tat ca open shadow roots; neu van fail, se luu screenshot + HTML + dialog JSON de lan test sau co DOM that de phan tich thay vi chi nhan `TimeoutException`. | 2026-03-31 |
| Tai asset noi bo tu control-plane cho render job phai bo gioi han read timeout | Worker dang dung `httpx.Client(timeout=20.0)` chung cho heartbeat/claim va ca stream asset local, khien job render co the no `ReadTimeout` giua chung khi file lon hoac mang cham. | `download_local_asset` se stream voi `timeout=None`, tach loi download file lon khoi timeout control-plane ng?n c?a polling/heartbeat. | 2026-03-31 |
| Browser upload phai dump debug artifact ca khi khong tim thay nut Done | L?i `Khong tim thay nut Done...` dang xay ra tren worker-01 du worker-02 upload duoc, va khong co screenshot/HTML tai diem fail nen khong the ket luan Studio dang o man nao. | Job fail o pha post-metadata se luu screenshot + HTML + dialog JSON tuong tu nhu nhanh editor-timeout, giup tach loi theo profile/channel thay vi doan chung ve worker code. | 2026-03-31 |
| BOT quick-assignment se dung kho BOT trống chọn nhiều thay vì single-select + bảng summary tĩnh | Admin đang phải duyệt danh sách BOT quá dài và khối bên phải cũ không hỗ trợ thao tác trực tiếp, gây chậm khi cấp phát hàng loạt. | Màn `Danh sách BOT` giờ ưu tiên search + multi-select BOT trống, bulk assign cho manager, và vẫn giữ chặt rule `1 user -> 1 BOT` khi cấp trực tiếp cho user. | 2026-03-31 |